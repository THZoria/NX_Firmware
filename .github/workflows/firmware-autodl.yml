name: 🎮 Firmware Auto Downloader

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  check_update:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    outputs:
      new_version: ${{ steps.version_check.outputs.new_version }}
      firmware_version: ${{ steps.version_check.outputs.firmware_version }}
    steps:
      - name: 🔍 Check firmware version (Switch 1 only, >=21.0.0)
        id: version_check
        shell: bash
        run: |
          set +e
          RSS=$(curl -sL --retry 3 --fail https://yls8.mtheall.com/ninupdates/feed.php)
          if [ $? -ne 0 ] || [ -z "$RSS" ]; then
            echo "new_version=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          LATEST_VERSION=$(echo "$RSS" | tr -d '\n' | sed 's/<item>/\n<item>/g' | \
            grep 'sys=hac' | grep -oE '[0-9]+\.[0-9]+\.[0-9]+' | sort -V | tail -n 1)

          if [ -z "$LATEST_VERSION" ]; then
            echo "new_version=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          MAJOR=$(echo "$LATEST_VERSION" | cut -d. -f1)
          if [ "$MAJOR" -lt 21 ]; then
            echo "new_version=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          HTTP_STATUS=$(curl -s -o /dev/null -w "%{http_code}" \
            -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            "https://api.github.com/repos/${{ github.repository }}/releases/tags/$LATEST_VERSION")

          if [ "$HTTP_STATUS" = "200" ]; then
            echo "new_version=false" >> "$GITHUB_OUTPUT"
          else
            echo "new_version=true" >> "$GITHUB_OUTPUT"
            echo "firmware_version=$LATEST_VERSION" >> "$GITHUB_OUTPUT"
          fi
          set -e

  download_and_release:
    needs: check_update
    if: needs.check_update.outputs.new_version == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: write

    steps:
      - name: ⬇️ Checkout code
        uses: actions/checkout@v6

      - name: 🐍 Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.x'

      - name: ⚙️ Install required Python modules
        run: |
          pip install requests anynet cryptography tqdm

      - name: ⬇️ Setup hactool-linux
        run: |
          if [ -f "hactool-linux" ]; then
            cp hactool-linux hactool
            chmod +x hactool
          else
            echo "Error: hactool-linux non trouvé."
            exit 1
          fi

      - name: 💻 Execute download script & Extract Release Notes
        id: download
        run: |
          VERSION="${{ needs.check_update.outputs.firmware_version }}"
          
          python3 firmware_downloader.py | tee script_output.log
          echo "firmware_version=$VERSION" >> "$GITHUB_OUTPUT"
          
          VER_RAW=$(grep "Internal version:" script_output.log | sed -E 's/.*Internal version: ([0-9]+).*/\1/')
          echo "VER_RAW=$VER_RAW" >> "$GITHUB_ENV"
          
          sed -n '/Archive created:/,$p' script_output.log > changelog_body.txt
          
          EOF=$(dd if=/dev/urandom bs=15 count=1 status=none | base64)
          echo "CHANGELOG_CONTENT<<$EOF" >> "$GITHUB_ENV"
          cat changelog_body.txt >> "$GITHUB_ENV"
          echo "$EOF" >> "$GITHUB_ENV"
          
      - name: 📦 Create Tag and Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VERSION: ${{ steps.download.outputs.firmware_version }}
        run: |
          V_N=$(( VER_RAW / 65536 ))
          
          ASSETS="Firmware ${VERSION}.zip"
          if [ -f "Firmware ${VERSION}.nsp" ]; then
            ASSETS="$ASSETS Firmware ${VERSION}.nsp"
          fi
          
          gh release create "$VERSION" $ASSETS \
            --title "Firmware $VERSION (v$V_N - $VER_RAW)" \
            --notes "Automatic download of the official Nintendo Switch firmware version $VERSION.
          ---
          **PS: The NSP file is intended for preservation purposes only. Please use the ZIP archive for emulators and Switch hardware instead.**
          
          Downloaded file details:
          
          ${CHANGELOG_CONTENT}"
